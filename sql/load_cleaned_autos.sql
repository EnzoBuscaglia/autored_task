CREATE TABLE IF NOT EXISTS autos_cleaned (
    make_id INTEGER,
    model_id INTEGER,
    year INTEGER,
    cylinder_capacity INTEGER,
    km INTEGER,
    color INTEGER,
    region INTEGER,
    price INTEGER,
    fuel_type_id INTEGER,
    transmission_id INTEGER,
    traction_id INTEGER,
    age INTEGER
);
